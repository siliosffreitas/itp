import 'package:flutter/material.dart';

class StopScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Detalhes da parada"),
      ),
      body: Container(),
    );
  }
}
